{
  "name": "gothamjs",
  "version": "1.0.0",
  "description": "GothamJS",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "clean": "lex clean && lerna run clean",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "update": "lex update -i"
  },
  "devDependencies": {
    "@types/node": "^10.12.18",
    "lerna": "^3.8.0"
  },
  "workspaces": [
    "packages/gothamjs",
    "packages/gothamjs-aws"
  ]
}
